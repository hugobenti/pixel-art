/**
 * Purpose:
 * Stable English strings for editor toasts so callers can dedupe by exact message + tone match.
 */

export const EDITOR_TOAST_HIDDEN_LAYER_MESSAGE =
  "The active layer is hidden. Show the layer to paint.";
