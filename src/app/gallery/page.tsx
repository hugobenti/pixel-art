/**
 * Purpose:
 * Gallery route — lists saved artworks and entry points to the editor.
 */
import { GalleryScreen } from "@/features/gallery/components/GalleryScreen";

export default function GalleryPage() {
  return <GalleryScreen />;
}
